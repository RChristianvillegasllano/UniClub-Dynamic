<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <link rel="icon" href="https://phshirt.com/wp-content/uploads/2022/01/UM-Tagum-College-1950.png" type="image/x-icon">
  <title>UniClub • University of Mindanao</title>

  <script src="https://cdn.tailwindcss.com"></script>
  <script>
    tailwind.config = {
      theme: {
        extend: {
          colors: {
            brandRed: '#c62828',
            brandOrange: '#ef6c00',
            ink: '#1f2933',
            subtle: '#6b7280',
          },
          fontFamily: {
            ui: ['Inter', 'system-ui', 'sans-serif'],
          },
        }
      }
    }
  </script>

  <style>
    @import url('https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700;800&display=swap');
    
    * {
      scroll-behavior: smooth;
    }

    body::before {
      content: "";
      position: fixed;
      top: 0;
      left: 0;
      width: 100%;
      height: 100%;
      background: url("/img/MfL6WWSk.jpg") center/cover no-repeat;
      background-attachment: fixed;
      filter: saturate(0.7) brightness(0.75);
      z-index: -2;
      pointer-events: none;
    }
    body::after {
      content: "";
      position: fixed;
      top: 0;
      left: 0;
      width: 100%;
      height: 100%;
      background: linear-gradient(135deg, rgba(198,40,40,0.94), rgba(239,108,0,0.88));
      mix-blend-mode: multiply;
      z-index: -1;
      pointer-events: none;
    }

    @keyframes fadeUp {
      from { opacity: 0; transform: translateY(40px); }
      to { opacity: 1; transform: translateY(0); }
    }
    
    @keyframes fadeIn {
      from { opacity: 0; }
      to { opacity: 1; }
    }


    .fade-up { animation: fadeUp 0.8s cubic-bezier(0.16, 1, 0.3, 1) forwards; }
    .fade-in { animation: fadeIn 1s ease-out forwards; }
    
    .glass {
      background: rgba(255, 255, 255, 0.1);
      backdrop-filter: blur(20px);
      border: 1px solid rgba(255, 255, 255, 0.2);
    }

    .glass-strong {
      background: rgba(255, 255, 255, 0.95);
      backdrop-filter: blur(20px);
      border: 1px solid rgba(255, 255, 255, 0.9);
    }

    .btn-primary {
      background: linear-gradient(135deg, #801515 0%, #d75a06 100%);
      transition: all 0.3s ease;
      box-shadow: 0 8px 24px rgba(198,40,40,0.4);
    }
    
    .btn-primary:hover {
      transform: translateY(-3px);
      box-shadow: 0 12px 32px rgba(198,40,40,0.6);
    }

    .btn-secondary {
      background: rgba(255, 255, 255, 0.15);
      backdrop-filter: blur(10px);
      border: 2px solid rgba(255, 255, 255, 0.3);
      transition: all 0.3s ease;
    }
    
    .btn-secondary:hover {
      background: rgba(255, 255, 255, 0.25);
      border-color: rgba(255, 255, 255, 0.5);
      transform: translateY(-2px);
    }

    .feature-card {
      transition: all 0.4s cubic-bezier(0.16, 1, 0.3, 1);
    }

    .feature-card:hover {
      transform: translateY(-8px);
      background: rgba(255, 255, 255, 0.15);
    }

    .stat-number {
      font-variant-numeric: tabular-nums;
    }

    /* Mobile menu styles */
    .mobile-menu {
      max-height: 0;
      overflow: hidden;
      opacity: 0;
      pointer-events: none;
      visibility: hidden;
      transform: translateY(-10px);
      z-index: 1000;
      padding: 0;
      margin-top: 0;
      background: rgba(30, 30, 30, 0.95);
      backdrop-filter: blur(20px);
      border: 1px solid rgba(255, 255, 255, 0.3);
      transition: max-height 0.4s cubic-bezier(0.4, 0, 0.2, 1), 
                  opacity 0.3s ease-out, 
                  visibility 0.3s ease-out, 
                  transform 0.3s ease-out,
                  padding 0.3s ease-out,
                  margin-top 0.3s ease-out;
    }

    .mobile-menu.open {
      max-height: 400px;
      opacity: 1 !important;
      pointer-events: auto !important;
      visibility: visible !important;
      transform: translateY(0);
      margin-top: 0.5rem;
    }

    /* Navbar scroll hide/show */
    nav {
      transition: transform 0.3s ease-in-out, opacity 0.3s ease-in-out;
    }

    nav.hidden {
      transform: translateY(-100%);
      opacity: 0;
      pointer-events: none;
    }
  </style>
</head>

<body class="min-h-screen font-ui text-white overflow-x-hidden relative">

  <!-- Navigation -->
  <nav id="main-nav" class="fixed top-0 left-0 right-0 z-50 fade-in">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 py-4 sm:py-6 relative">
      <div class="glass rounded-2xl px-4 sm:px-6 py-4 flex items-center justify-between">
        <!-- Logo -->
        <div class="flex items-center gap-2 sm:gap-3">
          <img src="/img/logo.png" alt="UM Logo" class="w-9 h-9 sm:w-11 sm:h-11 object-contain shadow-lg">
          <div class="leading-tight">
            <p class="text-[10px] sm:text-xs uppercase tracking-[0.2em] text-white/80 font-medium">UniClub</p>
            <p class="text-sm sm:text-base font-semibold text-white">University of Mindanao</p>
          </div>
        </div>

        <!-- Desktop Menu -->
        <div class="hidden md:flex items-center gap-8">
          <a href="#features" class="text-sm font-medium text-white/90 hover:text-white transition-colors">Features</a>
          <a href="#about" class="text-sm font-medium text-white/90 hover:text-white transition-colors">About</a>
          <a href="#contact" class="text-sm font-medium text-white/90 hover:text-white transition-colors">Contact</a>
        </div>

        <!-- CTA Buttons -->
        <div class="flex items-center gap-2 sm:gap-3">
          <a href="/student/login" class="hidden sm:block px-4 sm:px-5 py-2 sm:py-2.5 text-xs sm:text-sm font-medium text-white/90 hover:text-white transition-colors">
            Log In
          </a>
          <a href="/student/signup" class="hidden sm:block px-3 sm:px-5 py-2 sm:py-2.5 rounded-xl bg-white text-brandRed font-semibold text-xs sm:text-sm hover:bg-white/90 transition-all shadow-lg whitespace-nowrap">
            Get Started
          </a>
          <!-- Mobile Menu Button -->
          <button id="mobile-menu-btn" class="md:hidden p-2 text-white/90 hover:text-white transition-colors flex-shrink-0" aria-label="Toggle menu">
            <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path id="menu-icon" stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16M4 18h16"></path>
              <path id="close-icon" class="hidden" stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12"></path>
            </svg>
          </button>
        </div>
      </div>

      <!-- Mobile Menu -->
      <div id="mobile-menu" class="mobile-menu absolute top-full left-0 right-0 mt-2 mx-4 rounded-2xl z-50">
        <div class="flex flex-col gap-4 p-4">
          <a href="#features" class="text-sm font-medium text-white hover:text-white/80 transition-colors py-2">Features</a>
          <a href="#about" class="text-sm font-medium text-white hover:text-white/80 transition-colors py-2">About</a>
          <a href="#contact" class="text-sm font-medium text-white hover:text-white/80 transition-colors py-2">Contact</a>
          <a href="/student/login" class="text-sm font-medium text-white hover:text-white/80 transition-colors py-2 border-t border-white/30 pt-4">Log In</a>
        </div>
      </div>
    </div>
  </nav>

  <!-- Hero Section -->
  <section class="min-h-screen flex items-center justify-center px-4 sm:px-6 pt-24 sm:pt-32 pb-12 sm:pb-20">
    <div class="max-w-6xl mx-auto text-center">
      
      <!-- Badge -->
      <div class="fade-up inline-block mb-6 sm:mb-8">
        <div class="inline-flex items-center gap-2 px-3 sm:px-4 py-1.5 sm:py-2 rounded-full glass">
          <div class="w-2 h-2 rounded-full bg-green-400 animate-pulse"></div>
          <span class="text-xs sm:text-sm font-medium">Now Available for UM Students</span>
        </div>
      </div>

      <!-- Main Heading -->
      <h1 class="fade-up text-4xl sm:text-5xl md:text-6xl lg:text-7xl font-bold leading-tight mb-4 sm:mb-6" style="animation-delay: 0.1s;">
        Connect. Engage.<br class="hidden sm:block"/>
        <span class="bg-gradient-to-r from-white via-white to-white/80 bg-clip-text text-transparent">
          Experience UM.
        </span>
      </h1>

      <!-- Subheading -->
      <p class="fade-up text-base sm:text-lg md:text-xl text-white/90 max-w-2xl mx-auto mb-8 sm:mb-12 leading-relaxed px-4" style="animation-delay: 0.2s;">
        Your all-in-one platform for student organizations, campus events, and university life at the University of Mindanao.
      </p>

      <!-- CTA Buttons -->
      <div class="fade-up flex flex-col sm:flex-row items-center justify-center gap-3 sm:gap-4 mb-12 sm:mb-16 px-4" style="animation-delay: 0.3s;">
        <a href="/student/signup" class="btn-primary px-6 sm:px-8 py-3 sm:py-4 rounded-xl text-white font-semibold text-base sm:text-lg w-full sm:w-auto text-center">
          Create Your Account
        </a>
        <button onclick="scrollToFeatures()" class="btn-secondary px-6 sm:px-8 py-3 sm:py-4 rounded-xl text-white font-semibold text-base sm:text-lg w-full sm:w-auto">
          Explore Features
        </button>
      </div>
  </section>

  <!-- Features Section -->
  <section id="features" class="py-12 sm:py-20 px-4 sm:px-6">
    <div class="max-w-6xl mx-auto">
      
      <!-- Section Header -->
      <div class="text-center mb-12 sm:mb-16">
        <div class="inline-block px-3 sm:px-4 py-1.5 sm:py-2 rounded-full glass mb-3 sm:mb-4">
          <p class="text-xs sm:text-sm uppercase tracking-[0.2em] font-medium">Features</p>
        </div>
        <h2 class="text-3xl sm:text-4xl md:text-5xl font-bold mb-3 sm:mb-4">Everything You Need</h2>
        <p class="text-base sm:text-lg text-white/90 max-w-2xl mx-auto px-4">
          A complete platform designed to enhance your university experience
        </p>
      </div>

      <!-- Feature Cards -->
      <div class="grid sm:grid-cols-2 lg:grid-cols-3 gap-4 sm:gap-6">
        
        <!-- Feature 1 -->
        <div class="feature-card glass rounded-2xl p-6 sm:p-8">
          <div class="w-12 h-12 sm:w-14 sm:h-14 rounded-xl bg-gradient-to-br from-brandRed to-brandOrange flex items-center justify-center mb-4 sm:mb-6 shadow-lg">
            <svg class="w-6 h-6 sm:w-7 sm:h-7" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 20h5v-2a3 3 0 00-5.356-1.857M17 20H7m10 0v-2c0-.656-.126-1.283-.356-1.857M7 20H2v-2a3 3 0 015.356-1.857M7 20v-2c0-.656.126-1.283.356-1.857m0 0a5.002 5.002 0 019.288 0M15 7a3 3 0 11-6 0 3 3 0 016 0zm6 3a2 2 0 11-4 0 2 2 0 014 0zM7 10a2 2 0 11-4 0 2 2 0 014 0z"/>
            </svg>
          </div>
          <h3 class="text-lg sm:text-xl font-bold mb-2 sm:mb-3">Organization Management</h3>
          <p class="text-sm sm:text-base text-white/80 leading-relaxed">
            Join and manage student organizations with ease. Track memberships, roles, and activities all in one place.
          </p>
        </div>

        <!-- Feature 2 -->
        <div class="feature-card glass rounded-2xl p-6 sm:p-8">
          <div class="w-12 h-12 sm:w-14 sm:h-14 rounded-xl bg-gradient-to-br from-brandRed to-brandOrange flex items-center justify-center mb-4 sm:mb-6 shadow-lg">
            <svg class="w-6 h-6 sm:w-7 sm:h-7" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M8 7V3m8 4V3m-9 8h10M5 21h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v12a2 2 0 002 2z"/>
            </svg>
          </div>
          <h3 class="text-lg sm:text-xl font-bold mb-2 sm:mb-3">Event Calendar</h3>
          <p class="text-sm sm:text-base text-white/80 leading-relaxed">
            Never miss an event. Get real-time updates on campus activities, workshops, and social gatherings.
          </p>
        </div>

        <!-- Feature 3 -->
        <div class="feature-card glass rounded-2xl p-6 sm:p-8">
          <div class="w-12 h-12 sm:w-14 sm:h-14 rounded-xl bg-gradient-to-br from-brandRed to-brandOrange flex items-center justify-center mb-4 sm:mb-6 shadow-lg">
            <svg class="w-6 h-6 sm:w-7 sm:h-7" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 17h5l-1.405-1.405A2.032 2.032 0 0118 14.158V11a6.002 6.002 0 00-4-5.659V5a2 2 0 10-4 0v.341C7.67 6.165 6 8.388 6 11v3.159c0 .538-.214 1.055-.595 1.436L4 17h5m6 0v1a3 3 0 11-6 0v-1m6 0H9"/>
            </svg>
          </div>
          <h3 class="text-lg sm:text-xl font-bold mb-2 sm:mb-3">Smart Notifications</h3>
          <p class="text-sm sm:text-base text-white/80 leading-relaxed">
            Stay informed with personalized notifications about events, announcements, and updates from your organizations.
          </p>
        </div>

        <!-- Feature 4 -->
        <div class="feature-card glass rounded-2xl p-6 sm:p-8">
          <div class="w-12 h-12 sm:w-14 sm:h-14 rounded-xl bg-gradient-to-br from-brandRed to-brandOrange flex items-center justify-center mb-4 sm:mb-6 shadow-lg">
            <svg class="w-6 h-6 sm:w-7 sm:h-7" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12h6m-6 4h6m2 5H7a2 2 0 01-2-2V5a2 2 0 012-2h5.586a1 1 0 01.707.293l5.414 5.414a1 1 0 01.293.707V19a2 2 0 01-2 2z"/>
            </svg>
          </div>
          <h3 class="text-lg sm:text-xl font-bold mb-2 sm:mb-3">Document Hub</h3>
          <p class="text-sm sm:text-base text-white/80 leading-relaxed">
            Access important documents, forms, and resources shared by your organizations and university.
          </p>
        </div>

        <!-- Feature 5 -->
        <div class="feature-card glass rounded-2xl p-6 sm:p-8">
          <div class="w-12 h-12 sm:w-14 sm:h-14 rounded-xl bg-gradient-to-br from-brandRed to-brandOrange flex items-center justify-center mb-4 sm:mb-6 shadow-lg">
            <svg class="w-6 h-6 sm:w-7 sm:h-7" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M8 12h.01M12 12h.01M16 12h.01M21 12c0 4.418-4.03 8-9 8a9.863 9.863 0 01-4.255-.949L3 20l1.395-3.72C3.512 15.042 3 13.574 3 12c0-4.418 4.03-8 9-8s9 3.582 9 8z"/>
            </svg>
          </div>
          <h3 class="text-lg sm:text-xl font-bold mb-2 sm:mb-3">Communication Tools</h3>
          <p class="text-sm sm:text-base text-white/80 leading-relaxed">
            Connect with fellow students and organization leaders through integrated messaging and announcements.
          </p>
        </div>

        <!-- Feature 6 -->
        <div class="feature-card glass rounded-2xl p-6 sm:p-8">
          <div class="w-12 h-12 sm:w-14 sm:h-14 rounded-xl bg-gradient-to-br from-brandRed to-brandOrange flex items-center justify-center mb-4 sm:mb-6 shadow-lg">
            <svg class="w-6 h-6 sm:w-7 sm:h-7" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 19v-6a2 2 0 00-2-2H5a2 2 0 00-2 2v6a2 2 0 002 2h2a2 2 0 002-2zm0 0V9a2 2 0 012-2h2a2 2 0 012 2v10m-6 0a2 2 0 002 2h2a2 2 0 002-2m0 0V5a2 2 0 012-2h2a2 2 0 012 2v14a2 2 0 01-2 2h-2a2 2 0 01-2-2z"/>
            </svg>
          </div>
          <h3 class="text-lg sm:text-xl font-bold mb-2 sm:mb-3">Analytics Dashboard</h3>
          <p class="text-sm sm:text-base text-white/80 leading-relaxed">
            Track your involvement, attendance, and engagement across all your campus activities and organizations.
          </p>
        </div>

      </div>
    </div>
  </section>

  <!-- Stats Section -->
  <section class="py-12 sm:py-20 px-4 sm:px-6">
    <div class="max-w-6xl mx-auto">
      <div class="text-center mb-12 sm:mb-16">
        <h2 class="text-3xl sm:text-4xl md:text-5xl font-bold mb-4 text-white">
          Join Our Growing Community
        </h2>
        <p class="text-lg text-white/90 max-w-2xl mx-auto">
          Be part of the UniClub network at University of Mindanao
        </p>
      </div>
      
      <div class="grid grid-cols-1 sm:grid-cols-3 gap-8 sm:gap-12 text-center">
        <div class="fade-up">
          <div class="text-4xl sm:text-5xl md:text-6xl font-bold mb-3 bg-gradient-to-r from-brandRed to-brandOrange bg-clip-text text-transparent stat-number">
            <%= typeof stats !== 'undefined' ? stats.students.toLocaleString() : '0' %>
          </div>
          <div class="text-base sm:text-lg text-white/70 font-medium">Students</div>
        </div>
        <div class="fade-up" style="animation-delay: 0.1s;">
          <div class="text-4xl sm:text-5xl md:text-6xl font-bold mb-3 bg-gradient-to-r from-brandRed to-brandOrange bg-clip-text text-transparent stat-number">
            <%= typeof stats !== 'undefined' ? stats.clubs.toLocaleString() : '0' %>
          </div>
          <div class="text-base sm:text-lg text-white/70 font-medium">Organizations</div>
        </div>
        <div class="fade-up" style="animation-delay: 0.2s;">
          <div class="text-4xl sm:text-5xl md:text-6xl font-bold mb-3 bg-gradient-to-r from-brandRed to-brandOrange bg-clip-text text-transparent stat-number">
            <%= typeof stats !== 'undefined' ? stats.events.toLocaleString() : '0' %>
          </div>
          <div class="text-base sm:text-lg text-white/70 font-medium">Events</div>
        </div>
      </div>
    </div>
  </section>

  <!-- CTA Section -->
  <section id="about" class="py-12 sm:py-20 px-4 sm:px-6">
    
  <!-- Footer -->
  <footer id="contact" class="py-3 sm:py-4 px-4 sm:px-6 border-t border-white/10">
    <div class="max-w-6xl mx-auto">
      <div class="grid sm:grid-cols-2 lg:grid-cols-4 gap-4 sm:gap-5 mb-3 sm:mb-4">
        
        <!-- Brand -->
        <div class="sm:col-span-2">
          <div class="flex items-center gap-2 sm:gap-2 mb-1.5 sm:mb-2">
            <img src="/img/logo.png" alt="UM Logo" class="w-7 h-7 sm:w-8 sm:h-8 object-contain">
            <div class="leading-tight">
              <p class="text-[9px] sm:text-[10px] uppercase tracking-[0.2em] text-white/80 font-medium">UniClub</p>
              <p class="text-xs sm:text-sm font-semibold text-white">University of Mindanao</p>
            </div>
          </div>
          <p class="text-[10px] sm:text-xs text-white/70 max-w-sm">
            Connecting UM students with organizations, events, and opportunities across campus.
          </p>
        </div>

        <!-- Links -->
        <div>
          <h4 class="font-semibold mb-1.5 sm:mb-2 text-xs sm:text-sm">Quick Links</h4>
          <ul class="space-y-1 text-[10px] sm:text-xs text-white/70">
            <li><a href="#features" class="hover:text-white transition-colors">Features</a></li>
            <li><a href="#about" class="hover:text-white transition-colors">About</a></li>
            <li><a href="/student/login" class="hover:text-white transition-colors">Log In</a></li>
            <li><a href="/student/login" class="hover:text-white transition-colors">Get Started</a></li>
          </ul>
        </div>

        <!-- Contact -->
        <div>
          <h4 class="font-semibold mb-1.5 sm:mb-2 text-xs sm:text-sm">Contact</h4>
          <ul class="space-y-1 text-[10px] sm:text-xs text-white/70">
            <li>University of Mindanao</li>
            <li>Davao City, Philippines</li>
            <li>info@umindanao.edu.ph</li>
          </ul>
        </div>
      </div>

      <!-- Copyright -->
      <div class="pt-3 sm:pt-4 border-t border-white/10 text-center text-[10px] sm:text-xs text-white/70">
        <p>&copy; <span id="current-year">2024</span> UniClub. All rights reserved. University of Mindanao</p>
      </div>
    </div>
  </footer>

  <script>
    // Set current year dynamically
    document.getElementById('current-year').textContent = new Date().getFullYear();

    // Stats data from server
    const statsData = {
      students: <%- typeof stats !== 'undefined' ? stats.students : 0 %>,
      clubs: <%- typeof stats !== 'undefined' ? stats.clubs : 0 %>,
      events: <%- typeof stats !== 'undefined' ? stats.events : 0 %>
    };

    // Animate numbers on scroll
    function animateValue(element, start, end, duration) {
      let startTimestamp = null;
      const step = (timestamp) => {
        if (!startTimestamp) startTimestamp = timestamp;
        const progress = Math.min((timestamp - startTimestamp) / duration, 1);
        const current = Math.floor(progress * (end - start) + start);
        element.textContent = current.toLocaleString();
        if (progress < 1) {
          window.requestAnimationFrame(step);
        } else {
          element.textContent = end.toLocaleString();
        }
      };
      window.requestAnimationFrame(step);
    }

    // Observe stats section for animation
    const observerOptions = {
      threshold: 0.5,
      rootMargin: '0px'
    };

    const statsObserver = new IntersectionObserver((entries) => {
      entries.forEach(entry => {
        if (entry.isIntersecting) {
          const statNumbers = entry.target.querySelectorAll('.stat-number');
          statNumbers.forEach((stat, index) => {
            const targetValue = index === 0 ? statsData.students : 
                               index === 1 ? statsData.clubs : statsData.events;
            animateValue(stat, 0, targetValue, 1500);
          });
          statsObserver.unobserve(entry.target);
        }
      });
    }, observerOptions);

    // Start observing stats section when page loads
    window.addEventListener('DOMContentLoaded', () => {
      const statsSection = document.querySelector('.stat-number')?.closest('section');
      if (statsSection) {
        statsObserver.observe(statsSection);
      }
    });

    function scrollToFeatures() {
      document.getElementById('features').scrollIntoView({ behavior: 'smooth' });
    }

    // Mobile menu toggle
    const mobileMenuBtn = document.getElementById('mobile-menu-btn');
    const mobileMenu = document.getElementById('mobile-menu');
    const menuIcon = document.getElementById('menu-icon');
    const closeIcon = document.getElementById('close-icon');

    if (mobileMenuBtn && mobileMenu) {
      mobileMenuBtn.addEventListener('click', (e) => {
        e.stopPropagation();
        e.preventDefault();
        const isOpen = mobileMenu.classList.contains('open');
        if (isOpen) {
          mobileMenu.classList.remove('open');
          if (menuIcon && closeIcon) {
            menuIcon.classList.remove('hidden');
            closeIcon.classList.add('hidden');
          }
        } else {
          mobileMenu.classList.add('open');
          if (menuIcon && closeIcon) {
            menuIcon.classList.add('hidden');
            closeIcon.classList.remove('hidden');
          }
        }
      });

      // Close mobile menu when clicking on a link
      const mobileLinks = mobileMenu.querySelectorAll('a');
      mobileLinks.forEach(link => {
        link.addEventListener('click', () => {
          mobileMenu.classList.remove('open');
          if (menuIcon && closeIcon) {
            menuIcon.classList.remove('hidden');
            closeIcon.classList.add('hidden');
          }
        });
      });

      // Close mobile menu when clicking outside
      document.addEventListener('click', (e) => {
        if (mobileMenu.classList.contains('open') && 
            !mobileMenu.contains(e.target) && 
            !mobileMenuBtn.contains(e.target)) {
          mobileMenu.classList.remove('open');
          if (menuIcon && closeIcon) {
            menuIcon.classList.remove('hidden');
            closeIcon.classList.add('hidden');
          }
        }
      });
    }

    // Navbar scroll hide/show functionality
    let lastScrollTop = 0;
    const nav = document.getElementById('main-nav');
    let ticking = false;

    window.addEventListener('scroll', () => {
      if (!ticking) {
        window.requestAnimationFrame(() => {
          const scrollTop = window.pageYOffset || document.documentElement.scrollTop;
          
          // Hide navbar when scrolling down, show when scrolling up
          if (scrollTop > lastScrollTop && scrollTop > 100) {
            // Scrolling down
            nav.classList.add('hidden');
          } else {
            // Scrolling up
            nav.classList.remove('hidden');
          }
          
          // Update background opacity based on scroll position
          if (scrollTop > 50) {
            nav.style.background = 'rgba(198, 40, 40, 0.1)';
          } else {
            nav.style.background = 'transparent';
          }
          
          lastScrollTop = scrollTop <= 0 ? 0 : scrollTop;
          ticking = false;
        });
        ticking = true;
      }
    });
  </script>

</body>
</html>
