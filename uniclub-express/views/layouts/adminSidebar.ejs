<!-- views/layouts/adminSidebar.ejs -->
<aside class="bg-white border border-gray-200 rounded-2xl shadow-sm flex flex-col overflow-hidden">
  <!-- Brand -->
  <div class="flex items-center gap-3 px-4 py-4 border-b border-gray-200">
    <img src="/img/logo.png" alt="UniClub" class="size-10 rounded-full object-cover" />
    <div>
      <div class="font-bold">UniClub</div>
      <div class="text-gray-500 text-sm">Admin Panel</div>
    </div>
  </div>

  <!-- Nav -->
  <nav class="flex-1 p-3 space-y-1">
    <a href="/admin/dashboard"
       class="flex items-center gap-3 px-3 py-2 rounded-xl
              hover:bg-gray-100
              <%= currentPath === '/admin/dashboard' ? 'bg-red-50 ring-1 ring-red-200' : '' %>">
      <span class="w-5 text-center">🏠</span>
      <span class="flex-1">Dashboard</span>
    </a>

    <a href="/admin/students"
       class="flex items-center gap-3 px-3 py-2 rounded-xl hover:bg-gray-100 <%= currentPath === '/admin/students' ? 'bg-red-50 ring-1 ring-red-200' : '' %>">
      <span class="w-5 text-center">👩‍🎓</span>
      <span class="flex-1">Students</span>
    </a>

    <a href="/admin/officers"
       class="flex items-center gap-3 px-3 py-2 rounded-xl hover:bg-gray-100 <%= currentPath === '/admin/officers' ? 'bg-red-50 ring-1 ring-red-200' : '' %>">
      <span class="w-5 text-center">👥</span>
      <span class="flex-1">Officers</span>
    </a>

    <a href="/admin/clubs"
       class="flex items-center gap-3 px-3 py-2 rounded-xl hover:bg-gray-100 <%= currentPath === '/admin/clubs' ? 'bg-red-50 ring-1 ring-red-200' : '' %>">
      <span class="w-5 text-center">🏛️</span>
      <span class="flex-1">Clubs</span>
    </a>

    <a href="/admin/analytics"
       class="flex items-center gap-3 px-3 py-2 rounded-xl hover:bg-gray-100 <%= currentPath === '/admin/analytics' ? 'bg-red-50 ring-1 ring-red-200' : '' %>">
      <span class="w-5 text-center">📊</span>
      <span class="flex-1">Analytics</span>
    </a>

    <a href="/admin/requirements"
       class="flex items-center gap-3 px-3 py-2 rounded-xl hover:bg-gray-100 <%= currentPath === '/admin/requirements' ? 'bg-red-50 ring-1 ring-red-200' : '' %>">
      <span class="w-5 text-center">📋</span>
      <span class="flex-1">Requirements/Activities</span>
    </a>

    <a href="/admin/messages"
       class="flex items-center gap-3 px-3 py-2 rounded-xl hover:bg-gray-100 <%= currentPath === '/admin/messages' ? 'bg-red-50 ring-1 ring-red-200' : '' %>">
      <span class="w-5 text-center">💬</span>
      <span class="flex-1">Communication</span>
      <% const unread = (messages || []).filter(m => !m.read).length; %>
      <% if (unread > 0) { %>
        <span class="px-2 py-0.5 text-xs font-semibold rounded-full bg-red-100 text-red-700"><%= unread %></span>
      <% } %>
    </a>

    <a href="/admin/reports"
       class="flex items-center gap-3 px-3 py-2 rounded-xl hover:bg-gray-100 <%= currentPath === '/admin/reports' ? 'bg-red-50 ring-1 ring-red-200' : '' %>">
      <span class="w-5 text-center">📄</span>
      <span class="flex-1">Reports</span>
    </a>
  </nav>

  <!-- Sign out -->
  <div class="p-3 border-t border-gray-200">
    <form method="post" action="/admin/logout">
      <button class="w-full px-3 py-2 rounded-xl bg-red-700 hover:bg-red-800 text-white font-semibold">
        Sign out
      </button>
    </form>
  </div>
</aside>
